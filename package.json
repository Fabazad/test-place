{
  "name": "test-place",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "engines": {
    "node": "16.20.1"
  },
  "scripts": {
    "postinstall": "cd server && npm i && cd ../client && npm i",
    "start-server": "cd server && npm start",
    "start-client": "cd client && npm start",
    "start": "cd server && npm start",
    "heroku-postbuild": "(rm -r ./server/build || true) && cd client && npm run build && mv build ../server/build",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "cd client && npm run build"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Fabazad/test-place.git"
  },
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/Fabazad/test-place/issues"
  },
  "homepage": "https://github.com/Fabazad/test-place#readme",
  "dependencies": {
    "error-stack-parser": "^2.1.4",
    "node-gyp": "^3.8.0"
  }
}
